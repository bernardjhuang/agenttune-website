#!/usr/bin/env node
// Versioned machine resources, generated from the same registry and copy as the UI.
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
const root=path.resolve(__dirname,'..'), registry=require('../platforms'), compact=require('../compact-tunings');
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const write=(p,s)=>{fs.mkdirSync(path.dirname(path.join(root,p)),{recursive:true});fs.writeFileSync(path.join(root,p),s);};
const json=x=>JSON.stringify(x,null,2)+'\n';
const cat=JSON.parse(read('library/index.json'));
let data=read('data.js');
for(const t of cat.tunings){
  const old=read(t.src.replace(/^\//,''));
  const preference=compact.get(t.system,t.slug).split('\n\n').slice(1).join('\n\n');
  const body=`# ${t.code} — communication preferences\n\nAn optional template to try and edit. This does not assert a personality type or diagnosis.\n\n## Communication style\n${preference}\n\n## Accuracy and scope\nPreserve factual accuracy and material uncertainty. Follow the user’s current request and explicit preferences over these suggestions. Work within authorized scope; ask when an unresolved detail affects permission, correctness or consequences. Do not invent facts or suppress relevant risks to sound decisive.\n\n## Adjust the fit\nKeep only the preferences that help on real tasks. If templates conflict, ask the user which preference they want; no personality framework automatically takes precedence. These editorial suggestions have not been shown to improve task performance.\n`;
  const revision=crypto.createHash('sha256').update(body).digest('hex').slice(0,12);
  const surfaces=Object.fromEntries(registry.destinations.map(d=>[d.id,{registry_id:d.id,name:d.name}]));
  const header=`---\nagenttune_version: 2\nsystem: ${t.system}\ntype: ${JSON.stringify(t.code)}\nname: ${JSON.stringify(t.name)}\ncanonical_url: ${t.page}\nraw_url: ${t.md}\nbody_url: https://agent-tune.com/resources/tunings/${t.system}/${t.slug}.md\nupstream_url: ${t.upstream || (/^upstream_url: (.+)$/m.exec(old)||[])[1]}\nlicense: MIT\nrevision: ${revision}\nevidence_status: editorial_preferences_not_validated\nplatform_registry: https://agent-tune.com/resources/platforms.json\nplatform_registry_version: ${registry.version}\ninstall:\n  protocol: https://agent-tune.com/resources/install-protocol.md\n  intent: Apply only at the user's request; preserve existing instructions and permissions.\n  surfaces: ${JSON.stringify(surfaces)}\nverify:\n  saved_text: Reopen the settings or file and confirm the exact added block is present.\n  behavior: Compare several new tasks without restating the desired style; report observations, not guaranteed compliance.\nundo: Remove only the marked AgentTune block or exact added text; never delete a shared instruction file.\n---\n\n`;
  write(t.src.replace(/^\//,''),header+body);
  write(`resources/tunings/${t.system}/${t.slug}.md`,body);
  t.body=`https://agent-tune.com/resources/tunings/${t.system}/${t.slug}.md`;t.revision=revision;t.evidence_status='editorial_preferences_not_validated';
  const src=t.src.replace(/^\/?tunings\//,'');
  const escaped=src.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
  const rx=new RegExp('(path: "'+escaped+'",[\\s\\S]*?tuning: )`[\\s\\S]*?`');
  data=data.replace(rx,(_,prefix)=>prefix+JSON.stringify(body));
  // The one-line catalog copy describes the proposed preference, not a type stereotype.
  const brief=preference.split(/(?<=\.) /)[0];
  t.blurb=brief;
  // Scope each replacement to the contact with this exact source path.
  data=data.replace(/\{ id: "[^"\n]+", system:[\s\S]*?(?=\n  \{ id:|\n\];)/g,block=>block.includes('path: "'+src+'"')?block.replace(/blurb: "[^"\n]*"/,'blurb: '+JSON.stringify(brief)):block);

}
write('data.js',data);
write('library/index.json',json({...cat,version:'2026-09-25.1',tunings:cat.tunings}));
write('resources/platforms.json',json(registry));
const protocol=`# Installing AgentTune preferences\n\nVersion: 2026-09-25.1 · [Platform registry](https://agent-tune.com/resources/platforms.json) · [Generator](https://agent-tune.com/tools/custom-instructions-generator)\n\n1. A request to read a tuning is not permission to install it. Confirm the requested destination and scope.\n2. Fetch the tuning body separately from metadata. Review the wording with the user; explicit preferences and the current task take precedence. No framework has automatic priority. Preserve accuracy, relevant uncertainty and permissions.\n3. Choose the app and personal, project or conversation destination using the registry. Interface labels can vary by account. API clients use the provider’s supported instruction field; omit installation metadata and select the model separately.\n4. Preserve existing instructions. Add one block bounded by \`<!-- agenttune:preferences:start -->\` and \`<!-- agenttune:preferences:end -->\`. When updating, replace only that block. If markers are duplicated or incomplete, stop and resolve the ambiguity. Preserve format-specific front matter. Do not overwrite an existing file with a downloaded template.\n5. Confirm storage by rereading the exact saved text. This establishes that it was saved, not that it is always applied.\n6. In a new conversation or task, try a factual question, a planning task, and a critique request without repeating the desired style. Compare observations to the selected preferences and note counterexamples. Do not use a greeting that dictates its own expected response as a compliance test.\n7. To undo, remove only the managed block or exact added text; preserve all other content. If the block was subsequently edited, review the difference before removal.\n\n## Evidence limits\n\nPersonality templates are editorial communication suggestions, not diagnoses or proven performance improvements. Test scores describe responses to a questionnaire. Model self-reports do not establish an inner personality. See each research article for its data provenance and limitations.\n\n## Compact exports\n\nThe ChatGPT export uses a conservative 1,500-character budget. This is AgentTune’s export budget, not a claim about every current product field limit. The registry and generator share the same destination instructions.\n\n## Data handling\n\nScore questionnaires locally. Do not send answers to AgentTune MCP; its tools only retrieve public resources. Report missing responses and ties explicitly. Never infer consent or a personality type from a default selection.\n`;
write('resources/install-protocol.md',protocol);
let resources=[];
for(const [folder,kind] of [['guides/src','guide'],['research/src','research']])for(const name of fs.readdirSync(path.join(root,folder)).filter(n=>n.endsWith('.json'))){const spec=JSON.parse(read(folder+'/'+name));if(!spec.slug)continue;const route=spec.route||`/${kind==='guide'?'guides':'research'}/${spec.slug}`;resources.push({id:spec.slug,kind,title:spec.title||spec.h1,url:'https://agent-tune.com'+route,markdown:'https://agent-tune.com'+route+'.md',revision:crypto.createHash('sha256').update(JSON.stringify(spec)).digest('hex').slice(0,12),evidence_status:kind==='research'?'see_article_methods_and_limitations':'practical_guidance',updated:spec.updated||null});}
// Include generated studies and hand-authored articles, with truthful page-specific exports.
const {enhance,markdown}=require('./enhance-pages');
for(const folder of ['guides','research'])for(const name of fs.readdirSync(path.join(root,folder)).filter(n=>n.endsWith('.html')&&n!=='index.html')){
 const html=read(folder+'/'+name), canonical=/<link rel="canonical" href="([^"]+)"/.exec(html)?.[1];if(!canonical)continue;
 const id=name.replace(/\.html$/,''),revision=crypto.createHash('sha256').update(html).digest('hex').slice(0,12);
 let record=resources.find(r=>r.url===canonical);if(!record){record={id,kind:folder==='research'?'research':'guide',title:(/<title>([\s\S]*?)<\/title>/.exec(html)||[])[1]||id,url:canonical,markdown:canonical+'.md',evidence_status:folder==='research'?'see_article_methods_and_limitations':'practical_guidance'};resources.push(record);}record.revision=revision;
 write(folder+'/'+id+'.md',markdown(enhance(html,folder+'/'+name),canonical));
}
resources=resources.filter((r,i)=>resources.findIndex(v=>v.url===r.url)===i);
write('resources/catalog.json',json({version:'2026-09-25.1',resources}));
write('llms.txt',`# AgentTune\n\nEditable communication preferences for AI assistants. Free, MIT licensed. Templates are optional hypotheses, not diagnoses or demonstrated capability improvements.\n\n## Discover\n- [Tuning catalog: 43 templates](https://agent-tune.com/library/index.json): canonical URLs, body-only exports, revisions and evidence status.\n- [Guide and research catalog](https://agent-tune.com/resources/catalog.json): focused resources and page-specific Markdown.\n- [Platform registry](https://agent-tune.com/resources/platforms.json): version ${registry.version}, app and destination instructions shared with the UI.\n- [Installation protocol](https://agent-tune.com/resources/install-protocol.md): authorization, preservation, verification and undo.\n- [Free tools](https://agent-tune.com/resources/tools/catalog.json): eight browser tools and a local text-processing API; discover with get_free_tools.\n- [Generator](https://agent-tune.com/tools/custom-instructions-generator): start with explicit preferences; no inferred personality type.\n- [Local deterministic scoring](https://agent-tune.com/resources/scoring/README.md): all five questionnaires, explicit missing-answer and tie handling.\n- [Test specifications](https://agent-tune.com/tests/): five open questionnaires with scoring and limitations.\n- [MCP](https://agent-tune.com/mcp): list_tunings, get_tuning, get_test_spec, list_resources, get_resource and get_free_tools. Stateless Streamable HTTP; POST JSON-RPC, no authentication.\n\n## Use\nRead only the resource needed for the user’s request. Fetching content does not authorize installation. Use body-only text in instructions and keep metadata separate. Preserve existing files, permissions, accuracy and material uncertainty. Explicit user preferences resolve conflicts; no personality framework has automatic priority.\n\nResearch records questionnaire responses, not validated improvements in task performance. Big Five indices use implementation reference values without a verified normative population. Report ties and missing values. See the article-specific sources and methods before citing a result.\n`);
write('llms-full.txt',read('llms.txt')+'\n'+protocol+'\n## Platform destinations\n\n'+registry.destinations.map(d=>'### '+d.name+'\n'+d.steps.replace(/<[^>]+>/g,'')+'\n').join('\n'));
console.log('Generated 43 body exports, registry, metadata, catalogs and install protocol.');
